# Prometheus scrape config for the SimForge stack (ADR-0030).
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: simforge-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: simforge-api
