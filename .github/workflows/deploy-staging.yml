name: deploy-staging
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4
      # WEEK 9: build+push images to ECR, prisma migrate deploy, fly/ecs deploy,
      # run scripts/smoke-test.sh, post-deploy contract verification.
      - run: echo "staging deploy pipeline (scaffold) — see docs/deploy.md"
