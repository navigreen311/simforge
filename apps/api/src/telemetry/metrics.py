"""Prometheus metrics (blueprint §H.2). Phase 1 = no-op configure; full metrics in Phase 9."""

from __future__ import annotations


def configure_metrics() -> None:
    # WEEK 9: register Prometheus collectors + expose /metrics.
    return None
